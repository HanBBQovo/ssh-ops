interface:
  display_name: "SSH Ops"
  short_description: "Remote SSH exec and SFTP transfers"
  default_prompt: "Use $ssh-ops to inspect configured hosts, run a safe remote command, or transfer files over SSH."
