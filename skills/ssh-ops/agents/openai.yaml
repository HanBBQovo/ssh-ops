interface:
  display_name: "SSH Ops"
  short_description: "直连 SSH、保存主机别名、传文件"
  default_prompt: "Use $ssh-ops to connect to a direct SSH target like user@host, save a reusable host alias, inspect configured hosts, run a safe remote command, or transfer files over SSH."
